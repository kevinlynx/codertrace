<% @title = user_follower_desc @user %>
<%= render 'shared/avatar-header', :user => @user %>
<div id="main">
  <h2><%= t :follower_msg, :name => @user.name, :count => @user.followers.count %></h2>
  <%= render 'shared/users', :users => @user.followers %>
</div>
