<%= form_for(@entry) do |f| %>
  <dl>
    <dt><%= f.label :title, t("entry.title") %></dt>
    <dd><%= f.text_field :title, :class => 'txt-ctrl', :size => 35 %></dd>
  </dl>
  <dl>
    <dt><%= f.label :url, t(:url) %></dt>
    <dd><%= f.text_field :url, :class => 'txt-ctrl', :size => 35 %></dd>
  </dl>
  <dl>
    <dt><%= f.label :rss_url, t(:rss_url) %></dt>
    <dd><%= f.text_field :rss_url, :class => 'txt-ctrl', :disabled => true, :size => 35 %></dd>
  </dl>
  <dl>
    <dt><%= f.label :description, t("entry.desc") %></dt>
    <dd><%= f.text_area :description, :size => "60x4", :class => 'txt-ctrl' %></dd>
  </dl>
  <dl>
    <dd>
      <%= link_to_submit t(:update), :class => 'link-btn' %>
      <%= link_to t("entry.delete"), @entry, :method => 'delete', :class => 'link-btn' %>
      <%= link_to t("entry.back"), user_path(@entry.user), :class => 'link-btn' %>
    </dd>
  </dl>
<% end %>
